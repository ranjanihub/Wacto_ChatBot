import Chatbot from "@/components/Chatbot";

export default function WidgetPage() {
  return (
    <div style={{
      width: "100%",
      height: "100vh",
      background: "transparent",
      overflow: "hidden"
    }}>
      <Chatbot />
    </div>
  );
}